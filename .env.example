# Base URL of your Metabase instance (no trailing slash)
METABASE_URL=https://metabase.example.com

# Prefer API key (Metabase 50+): Admin → Settings → API keys
METABASE_API_KEY=

# Or session token from POST /api/session (if not using API key)
# METABASE_SESSION_TOKEN=
